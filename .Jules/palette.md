## 2024-05-07 - Add loading spinner to async submit button
**Learning:** Providing visual feedback with a loading spinner (like `Loader2` from `lucide-react`) and disabling the button during asynchronous operations (like form submissions) prevents multiple submissions and improves perceived performance and user trust.
**Action:** Always include a loading state indicator alongside text for primary actions that require asynchronous processing.
